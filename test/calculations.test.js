const chai = require('chai');
const { expect } = chai;

const { getComputerMove, isGameWon, movesLeft } = require('./../calculations');

describe('calculations', () => {
  describe('movesLeft', () => {});
  describe('isGameWon', () => {});
  describe('getComputerMove', () => {});
});
